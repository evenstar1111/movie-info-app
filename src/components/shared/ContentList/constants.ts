import { RoutePrefixMap } from './ContentList.types';

/**
 *
 * TODO: have a system, like save the url segements in a object, globally stored
 */
export const routePrefixMap: RoutePrefixMap = {
   movie: 'movie',
   collection: 'collection',
   person: 'person',
   tv: 'tv',
};
